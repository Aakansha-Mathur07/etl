# Extract Transform Load

The project was a part of the CSCI 1270: Database Systems course at Brown University. 

Goal:
1. Write an application, import, which will use the csv files to populate a SQLite database.
2. Write an application, query, which will make pre-defined queries against the SQLite database and print the results to the console. 


Bugs:
There are no bugs in the project.
